/** Wersja widoczna w UI — zsynchronizuj z app.json `expo.version`. */
export const APP_VERSION = '1.0.0';

/** runtimeVersion dla expo-updates — musi się zgadzać z binarką IPA. */
export const RUNTIME_VERSION = APP_VERSION;
