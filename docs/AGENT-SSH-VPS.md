# VPS — Dostęp dla maintainera

Informacje o dostępie SSH do VPS (IP, klucze, deploy) zostały przeniesione do `.local-credentials/AGENT-SSH-VPS.md`.

Folder `.local-credentials/` jest dodany do `.gitignore` i **nie jest commitowany**.

Aby uzyskać dostęp do VPS, skontaktuj się z maintainerem projektu.
